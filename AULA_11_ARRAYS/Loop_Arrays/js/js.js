let numbers = [23, 19, 54, 76, 82]

for (let i = 0; i <= numbers.length; i++) {
   console.log(numbers[i])

   }

   let nomes = ['anady', 'fernando', 'maria', 'juliana']

   for (let i= 0; i<=nomes.length; i++) {
     console.log(nomes[i])
   }